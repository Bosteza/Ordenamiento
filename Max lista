int findMax(int b[], int l) {
    int max= -10000000;
    int index = -1;

    for (int i = 0; i < l; i++) {
        if (b[i] > max) {
            max= b[i];
            index = i;
        }
    }

    return index;
}
