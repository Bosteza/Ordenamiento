#include <stdio.h>
#include <time.h>
#include <stdlib.h>

//Se declara función

int main(void) {
	
    // Benchmark
    clock_t tic = clock();

    // Inicio función
    

    // Benchmark
    clock_t toc = clock();
    printf("\nElapsed: %f seconds\n", (double)(toc - tic) / CLOCKS_PER_SEC);

    return 0;
}

//Fin función
